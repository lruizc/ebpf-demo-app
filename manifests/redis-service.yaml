apiVersion: v1
kind: Service
metadata:
  name: redis
  namespace: weather-demo
  labels:
    app: redis
    app.kubernetes.io/name: redis
    app.kubernetes.io/part-of: weather-demo
spec:
  selector:
    app: redis
  ports:
    - port: 6379
      targetPort: 6379
  type: ClusterIP
